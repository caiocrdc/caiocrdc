name: 🐍 Gerar animação da cobrinha azul

on:
  schedule:
    - cron: "0 0 * * *"  # Gera diariamente à meia-noite
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  generate:
    name: Gerar e publicar animação
    runs-on: ubuntu-latest

    permissions:
      contents: write  # Necessário para publicar no branch output

    steps:
      - name: Clonar repositório
        uses: actions/checkout@v3

      - name: Gerar cobrinha animada
        uses: Platane/snk@v3
        with:
          github_user_name: caiocrdc
          outputs: dist/snake.svg
          palette: "github-dark"  
          color_snake: "68BBD9" # Azul da cobrinha
          color_dots: "30363d,244e75,337ab7,68BBD9"
          # 30363d → cinza GitHub Dark (sem commits)
          # 244e75 → azul escuro (poucos commits)
          # 337ab7 → azul médio (commits regulares)
          # 68BBD9 → azul claro (muitos commits)

      - name: Publicar no branch output
        uses: crazy-max/ghaction-github-pages@v3.1.0
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

